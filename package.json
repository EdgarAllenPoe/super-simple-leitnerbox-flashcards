{
  "name": "super-simple-leitnerbox-flashcards",
  "version": "1.1.0",
  "private": true,
  "description": "A dependency-free five-box Leitner flashcard web app.",
  "scripts": {
    "check": "node --check core.js && node --check app.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
